<!--
  Pre-filled by .github/PULL_REQUEST_TEMPLATE.md.
  Edit each section; remove this comment block when done.
-->

## Summary

<!-- What changed and why. 1-3 bullets is usually enough. -->
-

## Test plan

- [ ]

## Privacy

<!--
  Required by .github/workflows/lgpd-conformance.yml and gdpr-conformance.yml.
  The marker line must start the line (after optional `- `) and is case-insensitive.

  If your change DOES touch personal data (auth, analytics, networking, storage,
  CloudSync, personalization, PrivacyInfo.xcprivacy, Info.plist), replace
  `N/A (...)` with a one-line rationale covering: purpose, lawful basis,
  retention, deletion path. Example:
    LGPD review: new analytics event records article_id only (no user ID);
                 retained 30 days; cleared by clearAllUserData()
-->

LGPD: N/A (no personal data touched)
GDPR: N/A (no personal data touched)
CCPA: N/A (no personal data touched)
